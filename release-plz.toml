# release-plz: バージョン bump / タグ / GitHub Release を自動化
# crates.io に公開する場合は [workspace] に publish = true を設定し、
# リポジトリに CARGO_REGISTRY_TOKEN を追加する

[workspace]
publish = false
git_only = true
git_release_enable = true
semver_check = false

[[package]]
name = "minimum-viewer"
git_only = true
